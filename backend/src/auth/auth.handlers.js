const { AppLogger } = require("../utils/logger");

const loginHandler = (req, res) => {
    res.status(501).json({
        messages: 'TODO implemented'
    })
}

module.exports = {
    loginHandler
};